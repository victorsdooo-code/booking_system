# 2026-03-18 - Dev1 Memory
## Sprint 3 Frontend - Doctor Types UI + Enhanced Schedule

### Victor's Decisions (Critical):
1. **Doctor Types**: Fixed 3 types - 中醫/物理治療/西醫 (no custom types needed normally)
2. **Photo Upload**: Local storage using base64 data URLs (no external storage)
3. **Service Pricing**: REMOVED entirely (not needed for current scope)

### Doctor Types Management UI (admin.html)

#### Navigation & Structure:
- **Position**: 3rd sidebar item "🏥 醫生類型管理" (between 新增預約 and 排班管理)
- **Section ID**: `doctorTypesSection`
- **Table Columns**: 排序 | 類型名稱 | 狀態 | 操作

#### Modal Design (`#addDoctorTypeModal`):
- Type name input (中醫/物理治療/西醫)
- Sort order number input (default: 1)
- Active/inactive checkbox
- Standard modal footer with 取消/儲存 buttons

#### JavaScript Functions:
- `loadDoctorTypes()` - GET /api/admin/doctor-types, fallback to 3 defaults
- `renderDoctorTypesTable()` - Sorts by sortOrder, shows status badges
- `showAddDoctorTypeModal()` / `closeAddDoctorTypeModal()`
- `editDoctorType(id)` - Populates modal with existing type data
- `submitDoctorType()` - POST/PUT based on editingDoctorTypeId
- `toggleDoctorTypeStatus(id, newStatus)` - Toggle active/inactive
- `deleteDoctorType(id)` - DELETE with confirmation

#### State Variables:
- `allDoctorTypes = []` - Cached list
- `editingDoctorTypeId = null` - Track edit mode

---

### Enhanced Schedule UI (admin.html)

#### Batch Processing Section (NEW):
- **Container**: Added before schedule form in filters-container
- **Fields**:
  - `batchStartDate` - Date input
  - `batchEndDate` - Date input
  - `batchDayOfWeek` - Multi-select (0-6, 日-六)
- **Actions**:
  - `applyBatchSchedule()` - POST /api/admin/schedules/batch
  - `bulkDeleteSchedules()` - DELETE /api/admin/schedules/batch

#### Schedule Form Updates:
- **REMOVED**: `scheduleDoctor` dropdown (was doctorId based)
- **ADDED**: 
  - `scheduleDoctorType` dropdown (中醫/物理治療/西醫)
  - `scheduleDoctorName` text input (陳醫師/李醫師...)
- **KEPT**: Service, day of week, time slots, duration, max appointments

#### Schedule Table Updates:
- **NEW Column**: "類型" - Shows doctorType as status badge
- **UPDATED Column**: "醫生" - Shows doctorName instead of doctorId lookup
- **Colspan**: Changed from 9 to 10 columns

#### JavaScript Function Updates:
- `renderSchedulesTable()` - Now shows s.doctorType and s.doctorName
- `saveSchedule()` - Payload includes doctorType + doctorName (no doctorId)
- `clearScheduleForm()` - Clears new fields
- `editSchedule(id)` - Populates new fields
- `updateScheduleDoctorDropdown()` - Stub for future filtering (currently no-op)

#### Batch Payload Structure:
```javascript
{
  doctorType: string,
  doctorName: string,
  serviceId: number,
  startTime: string,
  endTime: string,
  slotDuration: number,
  maxAppointments: number,
  startDate: string,  // YYYY-MM-DD
  endDate: string,    // YYYY-MM-DD
  daysOfWeek: number[]  // [0,1,2,3,4,5,6]
}
```

---

### Enhanced Doctor Management (admin.html)

#### Changes from Previous Sprint:
- **REMOVED**: Price field from service rows in addServiceRow()
- **REMOVED**: Price from getServicesFromForm() payload
- **KEPT**: Type dropdown, photo upload, services (name + duration only)

#### Service Row Structure (Updated):
```html
<input type="text" class="service-name">  <!-- flex: 2 -->
<input type="number" class="service-duration">  <!-- 80px -->
<button class="btn btn-danger btn-sm">刪除</button>
```

---

### Booking Page Update (index.html)

#### Doctor Cards Enhancement:
- **Type Badge Colors**:
  ```javascript
  const typeColors = {
    '中醫': '#2D5F4C',      // Dark green (primary)
    '物理治療': '#4A8C73',  // Medium green
    '西醫': '#66BB6A'       // Light green (accent)
  }
  ```
- **Photo**: 80x80px circular, or default 👨‍⚕️ emoji avatar
- **Services**: Shows name + duration only (no price)

#### Doctor Type Filter (NEW):
- **Location**: Above doctor cards in Step 2
- **UI**: Dropdown with options: 全部類型/中醫/物理治療/西醫
- **Function**: `filterDoctorsByType()` sets `currentDoctorTypeFilter`
- **Behavior**: `renderDoctorOptions()` filters by type before rendering

#### State Variable:
- `currentDoctorTypeFilter = ''` - Empty means show all

---

### API Integration Summary:

#### New Endpoints:
- `GET /api/admin/doctor-types` - List all doctor types
- `POST /api/admin/doctor-types` - Create new type
- `PUT /api/admin/doctor-types/:id` - Update type
- `DELETE /api/admin/doctor-types/:id` - Delete type
- `POST /api/admin/schedules/batch` - Batch create schedules
- `DELETE /api/admin/schedules/batch` - Batch delete schedules

#### Updated Endpoints:
- `GET /api/admin/schedules` - Now returns doctorType + doctorName
- `POST /api/admin/schedules` - Now accepts doctorType + doctorName
- `GET /api/doctors` - Returns doctors with type info

---

### CSS/Styling Notes:

#### Status Badge Colors ( reused):
- `.status-confirmed` - Green background (#d4edda) for active/enabled
- `.status-cancelled` - Red background (#f8d7da) for inactive/disabled

#### Type Badge (inline styles):
- Uses typeColors object for dynamic background colors
- Same `.duration` class as service cards but with custom background

---

### Edge Cases Handled:
- Empty doctor types list → Falls back to 3 defaults (中醫/物理治療/西醫)
- No type selected in filter → Shows all doctors
- Batch operation with no days selected → Shows alert
- Doctor name is free text → No validation beyond required check
- Photo upload → Converts to base64 data URL for preview

---

### Files Modified:
- `admin.html` - ~400 lines added/modified
  - New doctor types section, modal, and ~200 lines of JS functions
  - Enhanced schedule section with batch processing (~150 lines)
  - Updated doctor services to remove price (~10 lines)
  - Updated navigation and showSection() function
- `index.html` - ~50 lines modified
  - Enhanced renderDoctorOptions() with type colors
  - Added doctor type filter dropdown
  - Added filterDoctorsByType() function
  - Removed price from service display

---

### Testing Checklist:
- [ ] Doctor types CRUD operations
- [ ] Doctor type sort order management
- [ ] Doctor type active/inactive toggle
- [ ] Schedule creation with type + name
- [ ] Batch schedule creation (date range + selected days)
- [ ] Batch schedule deletion
- [ ] Doctor list shows type badges
- [ ] Booking page doctor cards show type with correct colors
- [ ] Doctor type filter works on booking page
- [ ] No price fields visible anywhere
