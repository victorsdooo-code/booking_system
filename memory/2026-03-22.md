# 2026-03-22 - Sprint 2 Kickoff

## 🚨 CRITICAL FIX: Frontend API Requests All Failing (23:08)

**Issue:** Victor reported "介面可以，但 api 全部請求失敗的" (UI loads but all API requests failing)

**Root Cause:** API response format mismatch
- Backend returns: `{ success: true, data: [...] }`
- Frontend code: `clinics = data.clinics || data` → got object instead of array
- Render failed: `clinics.map()` threw error on object

**Fix:**
- Updated `apiRequest()` to return `data.data || data`
- Fixed all load functions to use `data` directly (removed `.xxx || data` fallbacks)
- Commit: `389eeda`

**Status:** ✅ FIXED and deployed to GitHub Pages

---

## Sprint 1 Status
✅ **v1.0.0 RELEASED** - All core features deployed

## Change Requests from Victor

### CR-001: 門店管理要加營業時間設定
**Status:** ✅ IMPLEMENTED

**Solution:**
- Replaced plain text business hours field with structured 7-day grid
- Each day has: checkbox (open/closed), open time dropdown, close time dropdown
- Time slots: 30-minute intervals from 09:00-21:00
- Data structure:
```javascript
businessHours: {
  monday: { isOpen: true, open: '09:00', close: '18:00' },
  tuesday: { isOpen: true, open: '09:00', close: '18:00' },
  // ... etc
}
```

### CR-002: 排班管理要關聯門店營業時間，先顯示可選醫生
**Status:** ✅ IMPLEMENTED

**Solution:**
1. **Doctor-Clinic Association:**
   - Added `clinicId` field to doctor records
   - Added "所屬門店" dropdown in doctor modal
   
2. **Schedule Modal Flow:**
   - Step 1: Select clinic → triggers `onClinicSelected()`
   - Shows business hours info for selected clinic
   - Filters doctor dropdown to show only doctors at that clinic
   - Step 2: Select date → triggers `onDateSelected()`
   - Validates clinic is open on that day
   - Generates time slots based on business hours
   - Step 3: Select doctor (filtered list)
   - Step 4: Select time (filtered by business hours)

## Technical Implementation

### Files Modified
- `admin.html` - Complete UI and logic updates

### Key Functions Added
- `onClinicSelected(clinicId)` - Filter doctors, show business hours
- `onDateSelected(clinicId, date)` - Validate day, generate time slots
- `updateTimeSlots(openTime, closeTime)` - Dynamic time slot generation
- `populateDoctorClinicSelect()` - Populate clinic dropdown in doctor modal

### Functions Updated
- `saveClinic()` - Now saves structured businessHours object
- `saveDoctor()` - Now includes clinicId
- `openClinicModal()` / `editClinic()` - Populate business hours UI
- `openDoctorModal()` / `editDoctor()` - Populate clinic dropdown
- `openScheduleModal()` - Reset business hours info display

### Mock Data Updated
- Clinics: Added structured businessHours
- Doctors: Added clinicId field

## Testing Plan
1. Edit clinic → Set business hours → Save
2. Create schedule → Select clinic → Verify doctors filtered
3. Select date → Verify time slots match business hours
4. Test closed day → Should alert "診所喺呢日無營業"

## Commit Message
```
Sprint 2: Add business hours UI + schedule association with clinic
```

## Notes
- Default hours: Mon-Fri 09:00-18:00, Sat-Sun closed
- Time slots generated in 30-minute intervals
- Cantonese alerts for user feedback
- Backwards compatible with existing data structure
