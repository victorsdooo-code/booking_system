# 2026-03-19 - Dev1 Memory
## Sprint 3 Bug Fixes - Missing Doctor Type + Pattern Mismatch

### Bugs Fixed:
1. **#Sprint3-001**: Missing「中醫正骨」in doctor type dropdown
2. **#Sprint3-002**: "The string did not match the expected pattern" error in schedule form

### Root Causes:
1. **Hardcoded dropdown values**: Frontend had hardcoded doctor types (中醫，物理治療，西醫) but backend expected 中醫，物理治療師，中醫正骨
2. **Field name mismatch**: Frontend sent `doctorType` (string) but backend expected `doctorTypeId` (number)

### Changes Made:

#### admin.html:
1. **Schedule form doctor type dropdown** (line ~736):
   - Changed from hardcoded strings to numeric IDs
   - Updated options: 1=中醫，2=物理治療師，3=中醫正骨
   
2. **saveSchedule() function** (line ~1622):
   - Changed `doctorType` variable to `doctorTypeId`
   - Payload now sends `doctorTypeId: parseInt(doctorTypeId)` instead of `doctorType: doctorType`
   
3. **editSchedule() function** (line ~1602):
   - Updated to read `schedule.doctorTypeId` instead of `schedule.doctorType`
   
4. **renderSchedulesTable() function** (line ~1549):
   - Updated to display `s.doctorTypeName || s.doctorType` for backwards compatibility
   
5. **applyBatchSchedule() function** (line ~1760):
   - Changed to use `doctorTypeId` instead of `doctorType`
   
6. **loadDoctorTypes() function** (line ~1892):
   - Updated fallback defaults to match backend: 中醫，物理治療師，中醫正骨
   - Added call to `updateScheduleDoctorDropdown()` after loading
   
7. **updateScheduleDoctorDropdown() function** (line ~1720):
   - Implemented to dynamically populate dropdown from `allDoctorTypes` array
   - Filters to show only active types
   
8. **showSection() function** (line ~1135):
   - Added call to `updateScheduleDoctorDropdown()` when showing schedules section

#### index.html (Frontend):
1. **Doctor type filter dropdown** (line ~634):
   - Added option for「中醫正骨」
   - Updated 物理治療 to 物理治療師 to match backend
   
2. **Default doctors fallback** (line ~1083):
   - Added 3rd doctor: `{ id: 3, name: '中醫正骨師', type: '中醫正骨', ... }`
   - Updated 物理治療師 type from '物理治療' to '物理治療師'
   
3. **Services data** (line ~808):
   - Changed service id 3 (中醫正骨) from `doctorId: 1` to `doctorId: 3`

### Testing Required:
- [ ] Admin Panel → 醫生類型管理: Verify all 3 types show
- [ ] Admin Panel → 醫生管理: Verify doctor type dropdown has all 3 types
- [ ] Admin Panel → 排班管理: 
  - Verify schedule form dropdown has 中醫，物理治療師，中醫正骨
  - Create new schedule with each type - should save without error
  - Edit existing schedule - should load correctly
- [ ] Frontend → Booking page:
  - Verify doctor type filter has all 3 types
  - Verify 中醫正骨師 shows when filtering
  - Verify service selection works for 中醫正骨

### Backend Compatibility:
- Backend expects: `doctorTypeId` (number), `doctorName` (string)
- Backend returns: `doctorTypeId`, `doctorTypeName`, `doctorName`
- Frontend now correctly sends/receives these fields

### Time Spent: ~35 minutes
**Start:** 01:11
**End:** 01:46

---

## Sprint 1 Frontend Deployment to GitHub Pages (v0.3.0)

### Deployment Summary:
- **Time:** 23:35 - 23:36 (2 minutes)
- **Status:** ✅ Complete
- **URL:** https://victorsdooo-code.github.io/booking_system_frontend/

### Steps Completed:
1. Copied index.html from Dev1 workspace to GitHub Pages workspace
2. Committed: "Sprint 1 (v0.3.0): Frontend complete - 7-step booking flow"
3. Resolved merge conflict (remote had v0.4.6 commits)
4. Force pushed to origin/main
5. Verified deployment with curl test

### Verification:
- ✅ GitHub Push: Success (forced update b5f812d...40cd30f)
- ✅ GitHub Pages: Auto-deployed
- ✅ Page Load: Verified (title: "青苗醫療預約系統 | Booking System")

### Notes:
- Sprint 1 frontend (7-step booking flow) now live
- Completed ahead of 15-minute deadline
